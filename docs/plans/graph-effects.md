# Optional graph effects

Christopher requested a special graph effect during the earlier Train discussion. Its trigger, duration and animation remain undecided. The accepted [Atlas](../atlas.md) is part of the combined 0.1.0 update and does not depend on selecting this optional effect.

Keep this idea on the existing later optional frontend route. Public-site and dashboard prototypes both finish before the Web Demo prototype, which precedes optional frontend implementation. The separate inner-graph prototype was removed at Christopher's request because reconciliation completed the design. Its removal neither selects nor cancels the optional effect and adds no new required design stage.
