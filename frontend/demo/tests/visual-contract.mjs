import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
const read = path => readFileSync(new URL('../'+path, import.meta.url),'utf8');
// Source checks for active production components. Browser reviews establish appearance.
const app=read('src/App.vue'), entry=read('src/main.js');
const reader=read('src/sanctuary/text/TextRealm.vue');
const tree=read('src/sanctuary/SanctuaryApp.vue');
const globalNotes=read('src/sanctuary/GlobalNotes.vue');
const folio=read('src/sanctuary/RealmFolio.vue');
const tools=read('src/sanctuary/text/NotebookTools.vue');
const search=read('src/components/SearchPanel.vue');
assert(app.includes('SanctuaryApp'));
assert(entry.includes('import.meta.env.DEV'));
assert(!tree.includes('https://theumst.com/'));
assert(tree.includes('GlobalNotes') && tree.includes('@notes="openGlobalNotes"'));
assert(globalNotes.includes("demoFetch('/api/demo/notes')") && globalNotes.includes('NotesDesk'));
assert(reader.includes('RealmFolio') && folio.includes('backdrop-filter:blur'));
assert(reader.includes(':focus-visible') && reader.includes('prefers-reduced-motion'));
assert(reader.includes('max-width:900px') && reader.includes('max-width:540px'));
assert(tools.includes(':aria-label') && tools.includes(':aria-pressed'));
assert(search.includes("$emit('retry')"));
assert(read('src/sanctuary/BriefContents.vue').includes('ContentsTree'));
assert(!read('src/sanctuary/books.ts').includes('Real Analysis'));
console.log('Production Demo source checks passed; rendered behavior is covered by browser review.');
